# ═══════════════════════════════════════════════════════════════════════════════
# SnapDog2 Supervisor Configuration - Streamlined Service Management
# ═══════════════════════════════════════════════════════════════════════════════
# Aligned with SnapDog2's unified configuration system and elegant architecture
# ═══════════════════════════════════════════════════════════════════════════════

[supervisord]
nodaemon=true
user=root
loglevel=info
logfile=/dev/stdout
logfile_maxbytes=0
pidfile=/run/supervisord/supervisord.pid
childlogdir=/var/log/supervisor

[unix_http_server]
file=/run/supervisord/supervisor.sock

[supervisorctl]
serverurl=unix:///run/supervisord/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

# ═══════════════════════════════════════════════════════════════════════════════
# 🎵 Core Audio Services
# ═══════════════════════════════════════════════════════════════════════════════

[program:snapserver]
command=/usr/local/bin/snapserver -c /etc/snapserver.conf --configdir=/var/lib/snapserver
autostart=true
autorestart=true
priority=10
user=snapcast
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
stdout_events_enabled=true
stderr_events_enabled=true

[program:shairport-sync]
command=/usr/local/bin/shairport-sync --configfile=/etc/shairport-sync.conf
autostart=%(ENV_SNAPDOG_AIRPLAY_ENABLED)s
autorestart=true
priority=15
user=snapcast
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
stdout_events_enabled=true
stderr_events_enabled=true

# ═══════════════════════════════════════════════════════════════════════════════
# 🔧 Configuration Management
# ═══════════════════════════════════════════════════════════════════════════════

[program:config-monitor]
command=bash -c '
    echo "🔧 SnapDog2 Configuration Monitor started";
    while true; do
        sleep 300;
        echo "📊 Services status check...";
        if ! pgrep -f snapserver >/dev/null; then
            echo "⚠️ Snapserver not running - supervisor will restart";
        fi;
        if [[ "${SNAPDOG_AIRPLAY_ENABLED:-true}" == "true" ]] && ! pgrep -f shairport-sync >/dev/null; then
            echo "⚠️ AirPlay service not running - supervisor will restart";
        fi;
    done
'
autostart=true
autorestart=true
priority=30
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
