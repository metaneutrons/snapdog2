# ═══════════════════════════════════════════════════════════════════════════════
# SnapDog2 Supervisor Configuration - Development Build
# ═══════════════════════════════════════════════════════════════════════════════
# Simplified configuration for development without D-Bus/Avahi dependencies
# ═══════════════════════════════════════════════════════════════════════════════

[supervisord]
nodaemon=true
user=root
loglevel=info
logfile=/dev/stdout
logfile_maxbytes=0
pidfile=/run/supervisord/supervisord.pid
childlogdir=/var/log/supervisor

[unix_http_server]
file=/run/supervisord/supervisor.sock

[supervisorctl]
serverurl=unix:///run/supervisord/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

# ═══════════════════════════════════════════════════════════════════════════════
# 🎵 Core Audio Services - Development Mode
# ═══════════════════════════════════════════════════════════════════════════════

[program:snapserver]
command=/usr/local/bin/snapserver -c /etc/snapserver.conf --configdir=/var/lib/snapserver
autostart=true
autorestart=true
priority=10
user=snapcast
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
stdout_events_enabled=true
stderr_events_enabled=true

# ═══════════════════════════════════════════════════════════════════════════════
# 🔧 Development Monitoring
# ═══════════════════════════════════════════════════════════════════════════════

[program:dev-monitor]
command=/bin/bash -c "echo '🔧 SnapDog2 Development Monitor started'; while true; do sleep 60; echo '📊 Snapserver status check...'; if ! pgrep -f snapserver >/dev/null; then echo '⚠️ Snapserver not running - supervisor will restart'; fi; done"
autostart=true
autorestart=true
priority=30
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
