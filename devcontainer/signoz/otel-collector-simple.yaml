receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 1s
    send_batch_size: 1024
    send_batch_max_size: 2048

  resource:
    attributes:
      - key: service.namespace
        value: snapdog
        action: upsert
      - key: deployment.environment
        value: development
        action: upsert

exporters:
  # Debug exporter (replacement for deprecated logging exporter)
  debug:
    verbosity: normal
    sampling_initial: 5
    sampling_thereafter: 200

  # Export metrics to Prometheus format
  prometheus:
    endpoint: "0.0.0.0:8889"
    const_labels:
      environment: development
      service_namespace: snapdog

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [debug]
    
    metrics:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [debug, prometheus]
    
    logs:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [debug]

  extensions: []
  
  telemetry:
    logs:
      level: info
    metrics:
      address: 0.0.0.0:8888
