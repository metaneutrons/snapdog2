@page "/"
@rendermode InteractiveServer

<PageTitle>SnapDog2 - Multi-Room Audio Control</PageTitle>

<div class="container">
    <header class="header">
        <h1>🎵 SnapDog2</h1>
        <p>Multi-Room Audio Control System</p>
    </header>

    <main class="main-content">
        <div class="zones-grid">
            <div class="zone-card">
                <h3>🛋️ Living Room</h3>
                <div class="zone-controls">
                    <button class="btn btn-primary">▶️ Play</button>
                    <button class="btn btn-secondary">⏸️ Pause</button>
                    <input type="range" min="0" max="100" value="50" class="volume-slider" />
                </div>
            </div>

            <div class="zone-card">
                <h3>🍽️ Kitchen</h3>
                <div class="zone-controls">
                    <button class="btn btn-primary">▶️ Play</button>
                    <button class="btn btn-secondary">⏸️ Pause</button>
                    <input type="range" min="0" max="100" value="30" class="volume-slider" />
                </div>
            </div>

            <div class="zone-card">
                <h3>🛏️ Bedroom</h3>
                <div class="zone-controls">
                    <button class="btn btn-primary">▶️ Play</button>
                    <button class="btn btn-secondary">⏸️ Pause</button>
                    <input type="range" min="0" max="100" value="20" class="volume-slider" />
                </div>
            </div>
        </div>

        <div class="status-section">
            <h2>System Status</h2>
            <div class="status-grid">
                <div class="status-card">
                    <h4>Zones</h4>
                    <span class="status-value">3</span>
                </div>
                <div class="status-card">
                    <h4>Active</h4>
                    <span class="status-value">1</span>
                </div>
                <div class="status-card">
                    <h4>Clients</h4>
                    <span class="status-value">3</span>
                </div>
            </div>
        </div>
    </main>
</div>

<style>
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
        font-family: 'Orbitron', monospace;
        background: #0a0a0a;
        color: #fff;
        min-height: 100vh;
    }

    .header {
        text-align: center;
        margin-bottom: 40px;
        color: #00ff88;
    }

    .zones-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 40px;
    }

    .zone-card {
        background: #1a1a1a;
        border: 1px solid #333;
        border-radius: 8px;
        padding: 20px;
        color: #fff;
    }

    .zone-controls {
        display: flex;
        gap: 10px;
        align-items: center;
        margin-top: 15px;
    }

    .btn {
        padding: 8px 16px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-family: inherit;
    }

    .btn-primary {
        background: #00ff88;
        color: #000;
    }

    .btn-secondary {
        background: #666;
        color: #fff;
    }

    .volume-slider {
        flex: 1;
        margin-left: 10px;
    }

    .status-section {
        background: #1a1a1a;
        border: 1px solid #333;
        border-radius: 8px;
        padding: 20px;
        color: #fff;
    }

    .status-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 15px;
        margin-top: 15px;
    }

    .status-card {
        text-align: center;
        padding: 15px;
        background: #2a2a2a;
        border-radius: 4px;
    }

    .status-value {
        display: block;
        font-size: 2em;
        font-weight: bold;
        color: #00ff88;
        margin-top: 5px;
    }
</style>
